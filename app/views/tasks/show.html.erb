<h1><%= @task.title %></h1>
<%= link_to 'back', tasks_path%>
<%= link_to "Delete",
            task_path(@task),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
<%# <a data-confirm="Are you sure?" rel="nofollow" data-method="delete" href="/task/2">
  Delete the spoon
</a> %>

<%= link_to 'edit' , edit_task_path(@task) %>
